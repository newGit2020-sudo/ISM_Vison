<UserControl x:Class="MenuControl.Views.SequenceListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:viewmode="clr-namespace:MenuControl.ViewModes"     
             >
    <Grid>
        <!--<Grid.DataContext>
            <viewmode:SequenceListViewMode x:Name="_DataContext"/>
        </Grid.DataContext>-->
        <ListBox ItemsSource="{Binding ObservableCollection}">
            
        </ListBox>
    </Grid>
</UserControl>

